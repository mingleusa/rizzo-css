---
import DocsLayout from '../../../../layouts/DocsLayout.astro';
import CodeBlock from '../../../../components/CodeBlock.astro';
import AddComponentTabs from '../../../../components/AddComponentTabs.astro';

const copyableHtml = `<div class="scroll-area" style="height: 12rem; width: 14rem;">
  <div class="scroll-area__viewport" tabindex="0">
    <div style="padding: var(--spacing-4);">Long content here... (use fixed height so it scrolls)</div>
  </div>
</div>

<!-- Horizontal: add scroll-area--horizontal -->
<div class="scroll-area scroll-area--horizontal" style="width: 20rem; overflow: hidden;">
  <div class="scroll-area__viewport" tabindex="0"><div style="display: flex; gap: 1rem; padding: 1rem; width: max-content;">Item 1 Item 2 Item 3</div></div>
</div>`;
---

<DocsLayout title="Scroll Area — Vanilla" description="Scrollable area with themed scrollbar. Ensure Rizzo CSS is loaded.">
  <section>
    <h2>Scroll Area</h2>
    <p>Give the wrapper a fixed height (or width for horizontal). Use <code>scroll-area</code> and <code>scroll-area__viewport</code>. Add <code>scroll-area--horizontal</code> for horizontal scrolling.</p>
    <AddComponentTabs componentName="ScrollArea" />
    <div class="example">
      <div class="example-title">Live example (vertical)</div>
      <div class="scroll-area" style="height: 10rem; width: 12rem; border: 1px solid var(--border); border-radius: var(--radius-md);">
        <div class="scroll-area__viewport" tabindex="0">
          <div style="padding: var(--spacing-4);">
            <p style="margin: 0 0 var(--spacing-2) 0;">Line 1</p>
            <p style="margin: 0 0 var(--spacing-2) 0;">Line 2</p>
            <p style="margin: 0 0 var(--spacing-2) 0;">Line 3</p>
            <p style="margin: 0 0 var(--spacing-2) 0;">Line 4</p>
            <p style="margin: 0 0 var(--spacing-2) 0;">Line 5</p>
            <p style="margin: 0;">Line 6</p>
          </div>
        </div>
      </div>
    </div>
    <h3>HTML</h3>
    <CodeBlock code={copyableHtml} language="html" />
    <p><strong>Astro / Svelte:</strong> <a href="/docs/components/scroll-area">Astro</a> · <a href="/docs/svelte/components/scroll-area">Svelte</a></p>
  </section>
</DocsLayout>

<style>
  .example { margin: var(--spacing-6) 0; padding: var(--spacing-4); background: var(--background-alt); border: 1px solid var(--border); border-radius: var(--radius-lg); }
  .example-title { font-size: var(--font-size-sm); font-weight: var(--font-weight-medium); color: var(--text-dim); margin-bottom: var(--spacing-3); text-transform: uppercase; letter-spacing: var(--letter-spacing-wider); }
</style>
