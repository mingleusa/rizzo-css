---
import DocsLayout from '../../../layouts/DocsLayout.astro';
import Sheet from '../../../components/Sheet.astro';
import AddComponentTabs from '../../../components/AddComponentTabs.astro';
---

<DocsLayout title="Sheet" description="Slide-out panel (drawer) from top, right, bottom, or left. Matches shadcn-svelte Sheet.">
	<section>
		<h2>Sheet</h2>
		<p>Drawer panel. Props: <code>title</code>, <code>side</code> (top|right|bottom|left), <code>open</code>. Open/close via <code>openSheet_*</code> / <code>closeSheet_*</code> on window.</p>
		<AddComponentTabs componentName="Sheet" />
		<div class="example">
			<div class="example-title">Live Example</div>
			<Sheet id="sheet-demo" title="Side panel" side="right">
				<p>Sheet content goes here.</p>
			</Sheet>
			<button type="button" class="btn" onclick="window.openSheet_sheet_demo()">Open sheet</button>
		</div>
	</section>
</DocsLayout>
